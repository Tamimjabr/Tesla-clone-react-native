import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    width: '100%'
  },
  footer: {
    color: '#5c5e62',
    textAlign: 'center'
  }
})

export default styles
